<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
    </tr>
  </thead>

  <tbody>
    <% @contries.each do |contry| %>
      <tr>
          <td><%= link_to contry.name, contry %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('add'), new_contry_path, class: 'btn-primary btn' %>
