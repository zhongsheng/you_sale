<p id="notice"><%= notice %></p>

<h3>
    <%= @contry.name %>
</h3>

<%= will_paginate @products %>
<div class="card-columns text-center">
    <%= render @products  %>
</div>
<%= will_paginate @products %>
